{
  "name": "server",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest",
    "dev": "node --watch --env-file=.env src/server.js",
    "dev:local": "node --watch --env-file=.env --env-file=.env.local src/server.js",
    "start": "node --env-file=.env src/server.js",
    "populate": "node --env-file=.env scripts/populate.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.2.1",
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "supertest": "^7.1.4",
    "vitest": "^4.0.16"
  }
}
